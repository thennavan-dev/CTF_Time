import 'package:hive/hive.dart';

part 'user_settings.g.dart';

@HiveType(typeId: 1)
class UserSettings extends HiveObject {
  @HiveField(0)
  final int reminderMinutes;

  UserSettings({required this.reminderMinutes});
}
